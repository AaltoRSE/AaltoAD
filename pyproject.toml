[project]
name = "AaltoAD"
version = "0.1.0"
description = "Anomaly detection models and training code"
authors = [
]
dependencies = [
    "pandas",
    "tqdm",
    "matplotlib",
    "torch_geometric",
    "numpy",
    "SciencePlots",
    "scikit-learn",
    "openpyxl",
    "pytest",
    "torch",
    "scienceplots",
]

[tool.setuptools]
packages = ["AaltoAD"]
